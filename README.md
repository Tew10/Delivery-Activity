# Delivery-Activity
Java console application that calculates the sum and average of decimal numbers. The user defines the number of rows and columns of a matrix, enters the values, views all stored data, and gets the final results using arrays and loops.
